export class Match {
  datetime: number; // match.starttime.timestamp(),
  name1: string; // nameBlue,
  name2: string; // nameRed,
  score1: number; // match.scoreblue,
  score2: number; // match.scorered,
  win: boolean; //  win,
  rlmatchid: string; // match.rlmatchid
}
